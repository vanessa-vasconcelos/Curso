// function comparacao(num1, num2) {
//     if(num1 > num2) return `${num1} é maior.`
//         return `${num2} é maior.` 

    // OU 

    // return num1 > num2 ? num1 : num2;
// }

// OU

// const comparacao = (num1, num2) => {
//     return num1 > num2 ? num1 : num2
// }

// OU

const comparacao = (num1, num2) => num1 > num2 ? num1 : num2
console.log(comparacao(80, 20))